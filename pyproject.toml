[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ruijie_login"
version = "0.1.0"
description = "CLI login helper for Ruijie ePortal."
requires-python = ">=3.8"
dependencies = []

[project.scripts]
ruijie_login = "ruijie_login.cli:main"
